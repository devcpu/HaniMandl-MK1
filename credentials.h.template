// WiFi credentials - copy to credentials.h and fill in your values.
// credentials.h is gitignored and will not be committed.
// Without it the ESP starts in AP mode for WebUI configuration.

#define WIFI_SSID "your_ssid"
#define WIFI_PASSWORD "your_password"
#define WIFI_IP 192, 168, 1, 100
#define WIFI_GATEWAY 192, 168, 1, 1
#define WIFI_SUBNET 255, 255, 255, 0
#define WIFI_DNS1 192, 168, 1, 1
#define WIFI_DNS2 8, 8, 8, 8
